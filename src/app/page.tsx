import SearchHistory from "@/components/search-history";
import React from "react";

const Home = () => {
  return (
    <section>
      <SearchHistory />
    </section>
  );
};

export default Home;
